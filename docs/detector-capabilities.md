# Fixor detector capabilities — scope contract

## What this file is

This document is the authoritative scope contract for what Fixor's detectors do and do not detect. **Any public surface — landing page, README, mintlify docs, marketplace listing, outreach copy, blog posts, sales conversations — must not claim capability beyond what is enumerated here.** When in doubt, this file wins.

The contract has three goals:

1. **Pin the wedge.** Six business-logic vulnerability classes in Node/TypeScript. Anything outside this list is "not yet," not "soon."
2. **Make negative claims first-class.** The "does not claim" rows protect us when a sophisticated reviewer asks "does Fixor handle X?" — the honest answer needs to be reachable, not buried.
3. **Tie claims to measured baselines.** Every CLAIMS row is backed by fixture pass counts saved in `test-output/`. If a baseline is missing, the claim is degraded to "shipping but unbaselined."

## How to read each detector entry

- **CLAIMS** — what the detector will catch today, enumerated concretely (specific patterns, libraries, frameworks, languages where applicable).
- **DOES NOT CLAIM** — what the detector will not catch, enumerated concretely. If a customer asks "does it catch X" and X isn't on either side, the honest answer is "we haven't validated that, so no."
- **Measured baseline** — fixture pass count + the on-disk log it came from. Format: `positives_passed / negatives_skipped (combined / threshold)`. "Inline post-PR-N" means a test run captured in conversation, not yet saved as a baseline log (re-baseline pending).

---

## Detectors

### 1. auth-bypass (`auth-bypass-multi`)

**CLAIMS:**
- **Sentinel-string bypasses** in TS/JS/Python/Go/Ruby: hardcoded `"anonymous"`/`"admin"`/`"public"` defaults, `|| true`, `|| "admin"`, `?? "admin"`, JWT verification swallowed inside try/catch, `verify_signature=False` (Python), `DEFAULT_USER_ID` / `DEFAULT_ADMIN_ID` / `DEFAULT_ADMIN_EMAIL` fallbacks, Ruby `params[:x] || "admin"`. **A sentinel trigger is judged on a 24-line window, not the file** (DOES NOT CLAIM, the window row). Ruby sentinels reach this detector only through the GitHub App path or a CLI run with `--ext=rb`: the CLI's default extension set is ts/tsx/js/jsx/py/go (`DEFAULT_EXTENSIONS` in `src/cli/file-walker.ts`), so a default `npm run scan` never reads a `.rb` file.
- **Missing-middleware bypasses** on router-style frameworks: a destructive route declaration (`router.post("/users/delete", handler)`, `adminRouter.delete("/x", handler)`) with no authentication middleware in the argument list, where sibling routes on the same router DO pass an auth middleware. **Express-family by fixture coverage; router-style identifier syntax by regex** — the regex matches literal `router`/`app`/`api` identifiers OR any identifier ending in `Router`/`App`/`Api` (verbs: `get`/`post`/`put`/`delete`/`patch`/`use`/`all`, string-literal-path first arg required). Fastify, Koa, and Hono route declarations whose identifiers also match the regex shape (e.g., a `fastify.post(...)` callsite where `fastify` matches the `app` literal, or any `*App` / `*Api` / `*Router` framework wrapper) reach the LLM stage and are judged on the same missing-auth-arg criteria, but they have no positive fixture coverage and their judgment is therefore not baseline-anchored. The "by fixture" coverage is Express-family; the "by regex" reach is broader.
- **Missing-HOC-wrapper bypasses** on Next.js App Router / Remix v2 file-system-routed handlers: a destructive route exported as `export const POST = ...` / `export async function GET(...)` / `export default function PUT(...)` (App Router) or `export const action = ...` / `export const loader = ...` (Remix v2, added Phase E 2026-05-23) with NO higher-order function wrapper whose NAME convention suggests auth/admin enforcement (recognized as gated: `withAuth`, `withAdmin`, `requireAuth`, `requireAdmin`, `protect`, `secure`, `authMiddleware`, `requireUser` from remix-auth, or any identifier containing `auth` or `admin` as a substring; HOCs with `session` in the name count as gating ONLY when the handler body uses the session value for an authorization decision — 401 on missing session, or ownership filter keyed on `session.user.id` — and a session-substring HOC whose body merely sets a tracking cookie or fires analytics is NOT gating) AND no inline auth check in the handler body. The LLM stage performs the HOC-name-convention judgment; generic-named wrappers (`withRoute(...)`, `appWrapper(...)`) that hide auth invisibly are treated as unguarded by default. Remix `REMIX_HANDLER_DEF_RE` matches are bounded by an `isRemixRoutePath` path-aware filter (`/routes/` segment or `app/root.{ts,tsx}`) so utility modules under `app/lib/` / `app/utils/` exporting `loader`/`action` factory functions are not misrouted to the LLM. **Synthetic calibration:** Phase C measured 3 positive + 3 negative App Router fixtures, all 6/6 passed (log: `test-output/auth-bypass-app-router-phase-c-posttune.log`). Phase D added 2 positive + 2 negative fixtures exercising unfamiliar ApiKey-suffix wrapper names (`withAccountApiKey`, `withStatsApiKey`) that do not match the `auth`/`admin` substring rule. **Phase E added 3 positive + 3 negative Remix fixtures** (bare action destructive, withLogging non-auth, withSessionAnalytics A3-class on Remix shape; requireUser/getServerSession negatives; over-match utility under `app/lib/` proves the path-aware filter holds), all 6/6 passed (log: `test-output/phase-e-calibration-auth-bypass.log`). Phase D ApiKey-pair result: POS-16 flagged HIGH, NEG-15 and NEG-16 both correctly skipped; POS-15 returned MEDIUM confidence and was suppressed to the review queue per HIGH-only emit policy — the harness records this as a positive miss because it expected FLAG, but the LLM's reasoning was correct ("blindly deletes any label by `body.labelId` without an ownership filter"), so we accept POS-15's MEDIUM result as intended conservative behavior on unfamiliar wrapper-name + no-body-enforcement, NOT as a relabel of the fixture's expected outcome (the baseline number stays 31/32, not 32/32). The body-discriminator rule generalizes from session-substring HOCs to non-enumerated wrapper-context auth shapes — `request.apiAuth.emailAccountId` scope-filtering recognized as gating without name enumeration. **Customer-facing UX consequence: routes wrapped in unfamiliar ApiKey-suffix HOCs with no body enforcement surface at MEDIUM in the review queue, not as HIGH alerts in the main panel.** Phase D ApiKey-pair log: `test-output/auth-bypass-phase-d-api-key-fixtures.log`. **Real-world partial measurement:** the inbox-zero scan halted at **182 of 228 App Router routes (~80%, PARTIAL scan, halted for API budget after $5-7 burn against the CLI's $2.74 estimate — not a full-corpus sweep)**. Across the scanned 182 routes: **0 auth-bypass findings**, including all measured `withAuth` / `withAdmin` / `withEmailAccount` / `withEmailProvider` / `withError` wrapper shapes. Session-tightening rule held across 21 measured `withEmailAccount` instances; not measured at full corpus density — the unscanned 46 routes contain 45 additional `withEmailAccount` instances, so the rule was tested at ~32% of the corpus's `withEmailAccount` usage (21 of 66). Real-world log: `test-output/inbox-zero-api-scan-phase-d.stdout.log`.

- **Cross-file parent-layout guard recognition (Remix / React Router v7, Phase G 2026-05-28):** a route whose auth gate lives in an ancestor `_layout.tsx` loader (the common Remix flat-routes pattern, e.g. Documenso's `_authenticated+/_layout.tsx`) is recognized as gated for READ (loader) concerns, suppressing the cross-file false positive — but ONLY when (a) the layout loader blocks on failure (throw redirect / 401 / 403; a non-blocking session read is not a gate) and (b) the resolver STRUCTURALLY PROVES coverage via unbroken `+`-folder nesting with no trailing-underscore opt-out. Coverage that cannot be proven is dropped (route flags at natural severity), never suppressed. A destructive `action` is NEVER cleared by a parent layout loader (in Remix/RR v7 the action runs before parent loaders). Validated on the real Documenso clone (3/3 admin read routes: FP before, cleared after); resolver coverage-proof unit-tested 7/7; suppression/adversarial gate 6/6.
- **FastAPI / Flask-2.0 missing-auth route detection (Python slice 1, 2026-05-28):** a route decorated with `@app.METHOD` / `@router.METHOD` (lang-gated to `.py` via `FASTAPI_ROUTE_DEF_RE` + `isPythonPath`, so it cannot cross-fire with the JS route-def patterns) doing a destructive/sensitive op with NO auth-suggesting dependency and no inline check is flagged. **In-file Depends name-convention only this slice:** `Depends(get_current_user)` / `Depends(require_admin)` / `Security(get_current_active_user)` / an `@login_required` decorator / a `current_user: CurrentUser` annotated-type-alias dependency or any auth/admin/current_user/require/verify/login/permission-named dependency = gated; `Depends(get_db)` / `Depends(get_session)` (DB session) / `Depends(get_settings)` = NOT auth (the `get_session` ambiguity follows the TS session-substring rule: gates only if the body uses it for an authz decision). **Synthetic calibration:** 2 positive (bare destructive + `Depends(get_db)`-only adversarial) + 2 negative (`Depends(get_current_user)`, `Security(get_current_active_user)`) — all behaved correctly; full auth-bypass suite 21/21 negatives, 20/21 positives (the 1 miss is the pre-existing borderline ApiKey fixture, n=1). **Real-world validation (canonical `fastapi/full-stack-fastapi-template`, `backend/app/api/routes/`):** items/users/login/utils all correctly CLEARED (real `CurrentUser` + `get_current_active_superuser` + inline-403 idioms recognized, no FP); `private.py` flagged — see DOES NOT CLAIM (conditional-mount cross-file limitation).
- **Flask missing-auth route detection (Python Flask slice, 2026-05-28):** a route decorated with `@app.route(..., methods=[...])` (Flask-only `.route`, `FLASK_ROUTE_DEF_RE`) or the shared `@app.get`/`@app.post` shorthand, in a file whose imports show Flask, doing a sensitive op with no `@login_required` (or auth-named decorator) and no inline `current_user`/`g.user`/`session` check, is flagged. **FastAPI/Flask disambiguation** for the shared shorthand is by in-file imports (`from flask`/`flask_login` → Flask rubric case 6; `from fastapi` → FastAPI rubric case 5; neither → cannot attribute in-file → not flagged, cross-file out of scope). Calibration: full suite 23/23 negatives incl. the shorthand disambiguation anchor (Flask `@app.post`+`@login_required` cleared, not misjudged by the FastAPI rubric), bare-Flask-route positive flags. **Real-world validation (Flask `flaskr` tutorial):** prefilter fires on all 4 `@bp.route` decorators; `create`/`update`/`delete` (`@login_required`+`g.user`) and the public `index` read all correctly cleared — 0 false positives.

**DOES NOT CLAIM:**
- Fastify, Koa, Hono, NestJS Guards/Interceptors, Spring `@PreAuthorize`/`@Secured`, ASP.NET `[Authorize]`, GraphQL resolver-level auth.
- Declarative auth (Rails `routes.rb` `authenticated do ... end`).
- tRPC `protectedProcedure`, Next.js middleware-based auth, edge-runtime auth.
- **App Router cross-file auth (middleware matchers, `layout.tsx` / DAL gating) — NOT covered, named limitation addressed in a later phase.** Layout-level auth is recognized for Remix and React Router v7 only; App Router `layout.tsx` does not wrap Route Handlers and Next.js discourages layout-based authz, so layout coverage is not structurally provable, and middleware-matcher coverage parsing is deferred. Non-+-folder Remix conventions (classic dotted flat-routes, non-+ folders) yield UNVERIFIED coverage and are not suppressed.
- **Flask cross-file auth — NOT covered (Python slice 2):** a custom decorator (e.g. `@require_login` / `@my_admin`) whose auth/admin behavior is defined in another module — judged by decorator NAME convention only; verifying the decorator body is cross-file. And a route file that imports the `app`/blueprint from elsewhere AND imports no Flask/FastAPI primitive cannot be framework-attributed in-file (returns not-flagged rather than risk misattribution). Both are slice-2 with the deterministic-bounds principle reserved.
- **FastAPI / Flask cross-file auth — NOT covered this slice (Python slice 2):** (a) what the function passed to `Depends(...)` actually does (defined in another module), and a `Depends` whose resolution requires reading the dependency body; (b) router-level `dependencies=[Depends(...)]` declared where the `APIRouter` is created in a different file; (c) **conditional / environment-gated router mounting** — e.g. the full-stack-fastapi-template mounts its `private` router only `if settings.ENVIRONMENT == "local"` in `api/main.py`, so `private.py`'s authless user-creation route is local-only-by-design but flags at the file level (a cross-file conditional-mount false positive, same class as Express `router.use`-in-another-file). The slice-1 claim is in-file Depends name-convention only. The [[deterministic-safety-bounds]] principle is reserved for slice 2: any cross-file Depends/mount suppression goes in deterministic code, not the prompt.
- Auth gating applied via `router.use()` at file scope when the relevant `use()` call lives in a different file than the route declaration (the prefilter sees the route file in isolation).
- **Authorization correctness under present authentication — NOT judged (measured 2026-08-16).** Case 2 of the prompt asks one question of an Express route: whether an access check is present, as a middleware argument or as a same-file `router.use(requireAuth)`. The response schema's own `authPresent` field defines itself as distinct from whether the caller is authorized for the specific action, and nothing in the prompt asks that second question. So a router under blanket `requireAuth` whose one destructive route carries an inverted ownership conditional (`if (isHoldingBroker)` where `if (!isHoldingBroker)` was meant, so every other authenticated caller may file a claim carrying `amountCents` and `payeeAccount`) returned `isVulnerable: false` at `high` confidence. Its twin, one negation operator apart, produced near-identical reasoning, so the miss is attributable to the question the prompt asks and not to fixture text. Instrument: `claude-sonnet-4-6`, system prompt fingerprint `45a17ae07c26`, which is the fingerprint `main` ships as of 2026-09-11. Frequency: not claimed; n=1 per fixture (R1, R3). Invalidated by any change to `SYSTEM_PROMPT_FINGERPRINT` in `src/analysis-engine/detectors/auth-bypass.detector.ts`; a re-measurement then needs the same pair under the new fingerprint. Evidence: `docs/measurements/auth-bypass-pending-pairs-2026-08-16.json` and the four recordings beside it; the fixture pair is held out under `fixtures/_pending/auth-bypass-blanket-use/` (#176); the enabling branch `feat/auth-bypass-enable-pending-pairs` (#177, closed unmerged 2026-09-11) is retained as evidence.
- File-system-routed framework handlers wrapped in generic-named HOCs that hide auth invisibly — e.g., `export const POST = withRoute(handler)` where `withRoute` is not auth-suggesting by name but actually enforces auth in a sibling file. The HOC-name-convention judgment treats these as unguarded by default; cross-file analysis to verify what `withRoute` does is out of scope. Same shape as the Express `router.use(authMiddleware)`-in-a-different-file limitation above.
- Customer wrappers whose NAME suggests auth but whose IMPLEMENTATION is a no-op stub. A wrapper named `withAuth`, `withAdmin`, `requireAuth`, etc. whose implementation in its defining module is `(fn) => fn` (or any other no-op identity passthrough) ships unguarded — Fixor reads the route file in isolation, trusts the auth-suggesting name, judges as gated. Same shape as the cross-file `router.use(authMiddleware)` limitation above, applied to the HOC-stub variant. To verify, audit the wrapper's implementation in its defining file. **Measured + decided (H9, 2026-06-14):** resolving these imports to read the body was designed and measured but not built — a relative-only resolver reaches ~0% of real-world guards and zero stubs were observed across corpora; see §Cross-file guard resolution (H9) for the evidence and the reopen trigger.
- **A same-file defect outside the sentinel's 24-line window — NOT seen; a bound, measured.** `analyzeFile` picks `routeDefTrigger ?? triggers[0]`; only a route-def trigger earns the whole-file payload. Every sentinel trigger (`jwt_verify`, `or_true_literal`, `role_or_admin`, the anonymous/public/default-id shapes, all of them) is sent as lines trigger-8 through trigger+16 (`buildFunctionCodePayload` → `extractContextWindow`), so the model cannot see how the values it is shown were produced elsewhere in the file. **Measured 2026-09-10 at one pinned commit, `auth0/express-jwt@e9ed6d240d945fca8f8911c1f1d743dfe3f1f8e3`:** `lib/index.js` is 138 lines; the shipped detector triggers `jwt_verify` at line 100 (`jwt.verify(token, secret, options, …)`) and sends lines 92-115. The defect is at line 20, `if (!options || !options.secret) throw …`: the only validation the options ever receive, with no `algorithms` requirement, before `options` is passed verbatim into `jwt.verify` at line 100 — the algorithm-confusion shape fixed in express-jwt 6.0.0 by requiring `algorithms` (CVE-2020-15084). Line 20 is 72 lines above the window's first line. Captured from the shipped `AuthBypassDetector` with the model entry point intercepted at the module boundary; the same instrument reproduces, byte for byte, the recorded request for `positive/03-jwt-verify-swallowed.ts` in `fixtures/replay/auth-bypass-multi/`, which is its control. A second case on the same instrument, `calcom/cal.com@fb0149453e97a047f44ec21e99f9d8af420d8365` `packages/features/oauth/services/OAuthService.ts` (523 lines, trigger at 480, payload 472-495), shows the same payload shape with no defect asserted outside it. **The MECHANISM is trigger selection and is read from source; the FREQUENCY of sentinel files whose defect lies outside the window is unmeasured and is deliberately not asserted.** **Invalidation: this row goes false if sentinel triggers gain the whole-file payload, if `triggers[0]` selection changes, or if the window changes shape. Re-audit it in the commit that changes any of these.** Evidence: `docs/measurements/auth-bypass-sentinel-window-bound-2026-09-10.json`.
- Files whose path carries a test-, fixture-, example-, script-, migration-, seed- or demo-named segment are never read by auth-bypass; the rule is shared by all six detectors and is disclosed once, with its measurement, under "Out of scope across the board" (the `SKIP_PATH_RE` row).

**Measured baseline:** 44/45 (98%) post-H7 re-baseline (2026-06-13, full 45-fixture suite: 21/22 positives + 23/23 negatives; the one positive miss is the pre-existing POS-15 ApiKey MEDIUM suppressed to the review queue — see §Lane discipline above) — log: `test-output/auth-bypass-baseline-h7-2026-06-13.log`. Prior **31/32 (97%) post-Phase-D** baseline — log: `test-output/auth-bypass-phase-d-api-key-fixtures.log` (Phase D ApiKey pair added 2026-05-23: 15/16 positives caught + 16/16 negatives skipped, overall PASS per the harness's asymmetric criterion). **POS-15 is the one harness-recorded positive miss**: it returned MEDIUM confidence (suppressed to review queue) rather than the expected FLAG. We accept POS-15's MEDIUM result as intended conservative behavior on unfamiliar wrapper-name + no body enforcement, but we do NOT relabel the fixture's expected outcome to make the number look like 32/32 — the baseline number stays 31/32 and this row footnotes why. POS-16 flagged HIGH; NEG-15 and NEG-16 both skipped correctly. Phase C 28/28 baseline preserved at `test-output/auth-bypass-app-router-phase-c-posttune.log` for historical comparison. Pre-Phase-C Express-only baseline preserved at `test-output/auth-bypass-post-pr53-baseline.log`. Real-world partial measurement: `test-output/inbox-zero-api-scan-phase-d.stdout.log` — 182 of 228 inbox-zero App Router routes scanned (PARTIAL, halted for budget), 0 auth-bypass findings on the scanned 182; not a full-corpus sweep.

**ANNOTATION (2026-08-09) — 44/45 IS PRESERVED BECAUSE IT IS STILL THE CORRECT SCORE OF THE EMIT POLICY IT WAS MEASURED UNDER, AND THAT POLICY IS NO LONGER THE SHIPPED ONE.** Both 44/45 figures in this section, the one above and the one below, date from 2026-06-13 and are accurate for the policy then in force, under which a MEDIUM-confidence verdict routed silently to the review queue instead of being reported. **#152 (merged 2026-08-07) made a MEDIUM emit.** Under the shipped policy the same 45-fixture suite scores **45/45**, measured live at n=5 on run `31277606806` (2026-08-08, ref `c02b37fe`): positives 22/22 at >= 4/5, negatives 23/23 at 5/5, 185 calls, 0 no-verdict, 0 transport failures, 0 declarations used, fingerprint `45a17ae07c26` matching shipping. **POS-15 is the entire difference, and it is why the old number is kept rather than replaced.** Live it returns `vuln/medium` on four of five runs and `vuln/high` on the fifth. Under the shipped policy it flags 5/5; under the pre-#152 policy the four MEDIUM runs are discarded, it flags 1/5, it misses the >= 4/5 per-positive bar, positives fall to 21/22 and the suite lands on exactly 44/45 — the same number, the same fixture, the same mechanism. That arithmetic is the measurement of what #152 bought, so erasing the old figure would destroy the only quantity that shows the policy change was load-bearing. Two consequences for anyone citing this section: the review-queue suppression recorded for POS-15 above is not the current behaviour on that fixture, and the one-positive-miss residual is closed for auth-bypass by the emit-policy change alone, with no threshold altered and no fixture relabelled. Full record, including the resolved pre-registration and the counterfactual arithmetic: `docs/measurements/auth-bypass-stage3-2026-08-08.json`.

**Lane discipline (H7, 2026-06-13):** auth-bypass reports two structured lane facts alongside every verdict — `authPresent` (yes/no/unclear: is the caller authenticated?) and `operationKind` (admin/general) — and DEFERS a finding to admin-check's lane when, on a ROUTE-DEF trigger, `authPresent="yes"` AND `operationKind="admin"`. That shape is a missing-ADMIN-GATE defect (an authenticated-but-not-admin route performing a privileged op), which is admin-check's specialty, not an auth bypass. The suppression bound is deterministic detector code (per the deterministic-safety-bounds rule), NOT prompt prose: it is scoped to route-def triggers so sentinel/HOC bypasses (`|| true`, `role||"admin"`, swallowed `jwt.verify`) — auth-bypass's own lane — never defer, and it fails OPEN (only `authPresent="yes"` defers; `"no"`/`"unclear"` fire, since a missed admin-gate finding is worse than a redundant one). Deferrals are logged to the review queue (`category: "auth-bypass-lane-deferral"`), never silently dropped; admin-check (never suppressed) owns and reports the route. This is the auth-bypass/admin-check analogue of IDOR's lane discipline (below) and resolves the auth-bypass↔admin-check cross-fire measured on the real-shape corpus (express-saas `admin.js`, fastapi-saas `admin.py`: both detectors fired 5/5 pre-H7). **Acceptance (`test:auth-bypass-lane`, n=5, log `test-output/auth-bypass-lane-h7-2026-06-13.log`, 50/50 calls clean):** shape #2 (auth present + admin, no admin gate) → auth-bypass defers 0/5 while admin-check fires 5/5 — **Express AND FastAPI** (`current_user: CurrentUser` recognized as auth `authPresent=yes` 5/5, so the claim covers both frameworks, not Express-only); shape #3 (no auth + admin) → auth-bypass fires 5/5 with admin-check 5/5 (honest double-report preserved); shape #1 general missing-auth fires 5/5 and a clean authenticated route stays silent 5/5. Every lane fact was unanimous across n=5 (no flips). The full 45-fixture suite re-baselined **44/45** in the same state (only miss the pre-existing POS-15 ApiKey MEDIUM; log `test-output/auth-bypass-baseline-h7-2026-06-13.log`); admin-check + IDOR diff-confirmed untouched.

---

### 2. admin-check (`admin-check-multi`)

**CLAIMS:**
- **Hardcoded-admin shapes** in TS/JS/Python/Go: `email === "x@y.z"`, `email.endsWith("@domain")`, `email.includes("admin"|"owner"|"founder"|"root"|"superuser")`, Go `strings.HasSuffix(email, "@x")`, `ADMIN_EMAIL` / `admin_email` constants, `ADMIN_EMAILS` / `admin_emails` allowlist arrays, `DEFAULT_ADMIN_ID`, `DEFAULT_ADMIN_EMAIL`, `role || "admin"`, `role ?? "admin"`, `req.body|query|params.<role>` reads, `role === "admin"` string compare on a client-controlled value. **Three shapes in this row ship REGEX-ONLY: `DEFAULT_ADMIN_ID`, `DEFAULT_ADMIN_EMAIL` and `role ?? "admin"`** (`default_admin_id`, `default_admin_email`, `role_fallback_admin`, `tier: "literal"`). With `FIXOR_ADMIN_CHECK_LLM_OPT_IN` unset (the shipped default, `llmValidation` false) a match on one of those three is emitted directly at `confidence: "high"` / `severity: "critical"` carrying the pattern's hand-authored explanation, and no model call is constructed (`AdminCheckDetector.analyzeFile`, the per-pattern Option G branch; diagnostics record `preFilterReason: "llm-bypass"`); on those three the regex match IS the privilege fallback. **The seven email/role string shapes** (`email === "…@…"`, `email.endsWith("@…")`, `email.includes("admin"…)`, Go `strings.HasSuffix(email, "@…")`, `ADMIN_EMAILS` arrays, `ADMIN_EMAIL` constants, `req.body|query|params.<role>` reads) **reach the model since 2026-09-10**; until then they were regex-only, and the 30/30 and 45/45 figures below were measured under that routing (see the annotation there). **CORRECTION (2026-09-10):** the sentence this replaces, landed in #185 the same day, said eleven patterns carried `tier: "literal"`; the source held ten (the eleven was the Day 8 figure from `fixtures/admin-check/META.md`, repeated without a re-count; found by `grep -c` while preparing the move). The row on non-grant matches below records what the regex-only routing did and what the move measured.
- **Missing-admin-gate shapes** on router-style frameworks: a privileged route (role/tier change, user management, billing settings, `/admin/*` paths, privileged toggles) with no admin authorization check anywhere — neither `requireAdmin`/`isAdmin`/`adminOnly`/`verifyAdmin` middleware in the route arglist nor an inline check in the handler body — where sibling routes on the same router ARE admin-gated. Same regex-vs-fixture asymmetry as auth-bypass missing-middleware above: **Express-family by fixture coverage; router-style identifier syntax (`router`/`app`/`api` literals or any `*Router`/`*App`/`*Api` suffix) by regex.** Fastify/Koa/Hono shapes whose identifiers match the regex reach the LLM stage and are judged on the same missing-admin-gate criteria, but have no positive fixture coverage and are not baseline-anchored.
- **Missing-admin-HOC-wrapper shapes** on Next.js App Router / Remix v2 file-system-routed handlers: a privileged route exported as `export const PUT = ...` / `export async function POST(...)` / `export default function DELETE(...)` (App Router) or `export const action = ...` / `export const loader = ...` (Remix v2, added Phase E 2026-05-23) performing an administrative action (same destructive shapes as Express missing-admin-gate) AND wrapped in NO admin-suggesting HOC (recognized as gated: `withAdmin`, `requireAdmin`, `adminOnly`, `isAdmin`, `verifyAdmin`, `withAdminAuth`, `withRole("admin", ...)`, or any HOC identifier containing `admin` as a substring) AND no inline admin authorization check in the handler body AND no admin-suggesting helper call invoked in the body before the privileged operation (recognized as gating: helper-function identifiers like `requireAdmin`, `requireAdminRole`, `assertAdmin`, `checkAdmin`, `enforceAdminRole`, or any helper-call identifier containing `admin` as a substring; non-admin helper calls like `logAccess()`, `recordEvent()`, `trackRequest()`, or `withRateLimit()` do NOT count as gating even when invoked before the privileged op). General auth HOCs (`withAuth`, `requireAuth`, `withSession`, `protect`) are NOT sufficient — they enforce authentication, not admin authorization. Remix matches are bounded by the same `isRemixRoutePath` path-aware filter described in the auth-bypass section. **Synthetic calibration:** Phase C measured 4 positive + 4 negative App Router fixtures, all 8/8 passed (AC-P4/AC-N4 anchor the new helper-call name-convention rule symmetrically; log: `test-output/admin-check-app-router-phase-c-posttune.log`). **Phase E added 3 positive + 3 negative Remix fixtures** (withAuth + admin op, bare admin loader, withLogging + admin delete; withAdmin/DB-role-check negatives; over-match utility under `app/utils/`), all 6/6 passed (log: `test-output/phase-e-calibration-admin-check.log`). **Real-world partial measurement:** the inbox-zero scan halted at **182 of 228 App Router routes (~80%, PARTIAL scan, halted for API budget — not a full-corpus sweep)**. Across the scanned 182 routes: **0 admin-check findings**, including the corpus's `admin/*` routes. Helper-call rule (AC-P4/AC-N4 anchor) held end-to-end on real OSS within the scanned 80%. Real-world log: `test-output/inbox-zero-api-scan-phase-d.stdout.log`.

- **Cross-file parent-layout admin guard recognition (Remix / React Router v7, Phase G 2026-05-28):** an admin route whose admin gate lives in an ancestor `_layout.tsx` loader (e.g. Documenso's `admin+/_layout.tsx` calling `isAdmin(user)` then redirecting) is recognized as gated for READ (loader) concerns, suppressing the cross-file false positive — but ONLY when (a) the layout loader performs an ADMIN check and blocks on failure (a plain authentication layout is NOT sufficient for admin-check, mirroring the in-file `withAuth`-insufficient rule) and (b) the resolver STRUCTURALLY PROVES coverage via unbroken `+`-folder nesting. Unprovable coverage is never suppressed; a destructive `action` is never cleared by a parent loader. Validated on the real Documenso clone: 3/3 `admin+` read routes (stats, organisations.$id, users._index) were false positives before and cleared after.
- **FastAPI / Flask-2.0 missing-admin-gate (Python slice 1b, 2026-05-28):** a `@app.METHOD` / `@router.METHOD` route performing an ADMIN op (user management, role/tier change, billing settings, `/admin/*`, instance stats) is flagged unless gated. **In-file admin name-convention only:** an admin-suggesting dependency (`Depends(require_admin)` / `Depends(get_current_active_superuser)` / `Security(require_admin)` / any `admin`/`superuser`-substring dep, in the signature OR the route decorator's `dependencies=[...]`), a Flask `@admin_required`, or an inline `is_superuser`/`role`/RBAC check = gated. **A PLAIN auth dependency (`get_current_user` / `get_current_active_user` / `CurrentUser`) is NOT sufficient** — that is the Python looks-guarding-but-isn't case. Calibration 40/40 (incl. the auth-only-on-admin-op adversarial flagging and the inline-superuser negative clearing). **Real-world validation (full-stack-fastapi-template):** users.py admin routes gated by per-route `dependencies=[Depends(get_current_active_superuser)]` correctly cleared; private.py flagged (conditional-mount class, see DOES NOT CLAIM).
- **Flask missing-admin-gate (Python Flask slice, 2026-05-28):** a Flask `@app.route`/`@bp.route` (or shared shorthand, disambiguated by imports) admin op gated ONLY by `@login_required` (authenticated, NOT admin) with no `@admin_required` and no inline `current_user.is_admin`/`g.user.is_admin`/role check is flagged — the Flask looks-guarding-but-isn't case. `@admin_required` (admin-named decorator) or an inline admin check clears. Calibration 42/42 incl. the `@login_required`-on-admin-op adversarial flagging and `@admin_required` clearing.

**DOES NOT CLAIM:**
- Fastify, Koa, Hono, NestJS Guards, Spring `@PreAuthorize`, decorator-based RBAC (Python decorators, TS decorators outside Express).
- Declarative RBAC (Casbin policies, OPA, Cedar).
- tRPC `adminProcedure`, GraphQL resolver-level admin checks, Next.js middleware-based admin gating.
- **App Router cross-file admin auth (`middleware.ts` matchers, `layout.tsx` / DAL gating) — NOT covered, named limitation for a later phase.** Layout-level admin auth is recognized for Remix and React Router v7 only (see auth-bypass DOES NOT CLAIM for why App Router layouts are not structurally provable).
- **FastAPI / Flask cross-file admin auth — NOT covered (Python slice 2):** what an admin-named dependency actually does (defined in another module), router-level `dependencies=[Depends(require_admin)]` declared where the `APIRouter` is created in a different file, and conditional/environment-gated router mounting (same cross-file class as the auth-bypass FastAPI section). In-file admin name-convention only this slice.
- Admin gating applied via `router.use(requireAdmin)` at file scope when the `use()` call lives in a different file than the route declaration.
- File-system-routed framework handlers wrapped in generic-named HOCs that hide admin enforcement invisibly (e.g., `withRoute("admin", handler)` where the admin-ness lives in `withRoute`'s implementation in a sibling file). Same cross-file limitation shape as Express's `router.use(requireAdmin)`-in-a-different-file case above.
- Customer wrappers whose NAME suggests admin but whose IMPLEMENTATION does not enforce admin authorization. A wrapper named `withAdmin`, `requireAdmin`, `adminOnly`, `withAdminLogging`, `withAdminMetrics`, etc. whose body does not perform an admin role check ships unguarded — Fixor trusts the admin-suggesting name without reading the body, treating the wrapper as gated. Covers both no-op stubs (`(fn) => fn`) and named-but-not-enforcing implementations (logging-only, metrics-only, tracing-only). Same cross-file analysis limitation as the auth-bypass deceptive-stub class above. **By design — measured asymmetry with auth-bypass's session-substring tightening:** auth-bypass tightened its `session`-substring rule in Phase C with a body-discriminator clause because session-named wrappers in the wild often do non-auth work (`withSessionTracking`, `withSessionAnalytics`). Admin-substring tightening was NOT applied because corpus grep across calcom-smoke, documenso-smoke, convoform-smoke, grafana scans, and the inbox-zero Phase D log found **zero `withAdmin*` HOC wrappers with non-enforcing bodies** — admin-named HOCs in practice enforce admin. Re-evaluate when a customer or a future scan surfaces a real `withAdmin*` non-enforcing wrapper. **Measured + decided (H9, 2026-06-14):** the cross-file resolver that would read the wrapper body was designed and measured but not built — relative-only reach is ~0% on real repos and zero stubs were observed; see §Cross-file guard resolution (H9) for evidence and the reopen trigger.
- **Non-grant code that matches one of the three remaining literal-tier patterns is reported as an admin grant, at critical, with no adjudication — NOT distinguished; for the seven string-shape patterns this was true until 2026-09-10 and is now measured cleared.** On the shipped path the literal tier has no model stage, so for `default_admin_id`, `default_admin_email` and `role_fallback_admin` the regex match IS the finding; those three name a privilege fallback by construction and no non-grant use of them is on record. **The measured case that forced the move:** `calcom/cal.com@fb0149453e97a047f44ec21e99f9d8af420d8365` `packages/lib/isSmsCalEmail.ts`, three lines, `return email.endsWith("@sms.cal.com");`, an SMS-address predicate, emitted `admin_check_risk` at `critical`/`high` with zero model calls under the pre-move routing (2026-09-10, `docs/measurements/admin-check-literal-tier-bound-2026-09-10.json`). It is now `fixtures/admin-check/negative/22-sms-email-helper.ts` (verbatim), reaches the model through `email_endswith_at` at judgment tier, and **stayed clean 5/5 (isVulnerable:false on every run)** in the post-move n=5 run below. **The MECHANISM (tier field, no model stage) is read from source; the FREQUENCY of non-grant matches on the three remaining literal patterns is unmeasured and is deliberately not asserted.** **Invalidation: this row goes false if any of the three regains a model stage, if a fourth pattern becomes literal, or if `FIXOR_ADMIN_CHECK_LLM_OPT_IN` ceases to default false. Re-audit it in the commit that changes any of these.** Evidence: `docs/measurements/admin-check-literal-tier-bound-2026-09-10.json`.
- Files whose path carries a test-, fixture-, example-, script-, migration-, seed- or demo-named segment are never read by admin-check; the rule is shared by all six detectors and is disclosed once, with its measurement, under "Out of scope across the board" (the `SKIP_PATH_RE` row).

**Measured baseline:** 30/30 (100%) — log: `test-output/admin-check-app-router-phase-c-posttune.log` (Phase C post-tune, 2026-05-23: 11 Express + 4 App Router positives all flagged, 11 Express + 4 App Router negatives all correctly skipped). Pre-Phase-C Express-only baseline preserved at `test-output/admin-check-post-pr53-baseline.log` for historical comparison. Real-world partial measurement added 2026-05-23: `test-output/inbox-zero-api-scan-phase-d.stdout.log` — 182 of 228 inbox-zero App Router routes scanned (PARTIAL, halted for budget), 0 admin-check findings on the scanned 182; not a full-corpus sweep.

**ANNOTATION (2026-09-10) — 30/30 IS THE SCORE OF A 30-FIXTURE SUITE THAT NO LONGER EXISTS, AND THE CURRENT SUITE HAS ITS OWN MEASURED NUMBER.** The figure above dates from Phase C (2026-05-23; 11 Express + 4 App Router positives, 11 Express + 4 App Router negatives). Phase E (Remix), the Python slices (FastAPI, Flask) and later literal-tier fixtures brought the suite to 24 positives + 21 negatives, and THAT suite was measured live at n=5 on run `30821994020` (2026-08-03, ref `25fa0116`): **45/45**, positives 24/24 at >= 4/5, negatives 21/21 at 5/5, 150 priced calls for 225 fixture-runs (the 75 runs decided without a model call are the literal-tier bypass and the pre-model gates, which is the shipped path), 0 no-verdict, 0 transport failures, fingerprint `ed52ebe3db91`. The old figure is kept because it is still the correct score of the suite it was measured on; the new figure is what the shipped detector scores on the suite that exists. R3 applies to both: zero FP at n=5 is no FP at 20% resolution, not calibration. Full record: `docs/measurements/admin-check-stage3-2026-08-03.json`. **ANNOTATION (2026-09-10, second) — BOTH FIGURES ABOVE WERE MEASURED UNDER THE PRE-MOVE ROUTING, IN WHICH TEN PATTERNS WERE REGEX-ONLY.** On 2026-09-10 seven patterns moved to the judgment tier and `negative/22-sms-email-helper.ts` was added, so the suite is 24 positives + 22 negatives and 39 of 46 fixtures reach the model. **Measured the same day, locally, n=5, `FIXOR_HALT_USD` set (log `test-output/admin-check-tier-move-2026-09-10.log`): 46/46**, positives 24/24 at >= 4/5, negatives 22/22 at 5/5, 195 priced calls, measured $1.5523, fingerprint `ed52ebe3db91` (unchanged: the prompt did not move). **No stage-3 WORKFLOW run exists for the current routing**; the 2026-08-03 per-detector green counted toward F-004 was earned under the old routing and is not re-claimed here. R3 applies: zero FP at n=5 is no FP at 20% resolution, not calibration.

---

### 3. IDOR (`idor-multi`)

**CLAIMS:**
- DB read/write where a request-derived id (`req.params.id`, `req.body.id`, tRPC `input.id`, FastAPI path param, Rails `params[:id]`, Go chi `chi.URLParam`) flows into a query (`findUnique`, `findFirst`, `update`, `delete`, raw SQL, `pool.query`) with no ownership filter (`userId: ctx.session.user.id` or equivalent) in the `where` clause and no post-fetch ownership check. **Bounded by co-location, not by dataflow: the id and the query must both match inside the SAME file, within `PROXIMITY_THRESHOLD` lines of each other.** `findPatternHits` runs twice over one file's content — once for `SOURCE_PATTERNS`, once for `SINK_PATTERNS` — and `enumerateSinkPairs` pairs each sink with its nearest source inside that window. "Flows into" above describes the shape the LLM stage then judges, NOT a dataflow analysis the detector performs: nothing traces a value across a function boundary and nothing crosses a file boundary. See the first DOES NOT CLAIM row for what that excludes.
- Router frameworks covered by fixtures: Express, Next.js App Router (both Next.js 14 destructured-params `{ params: { id: string } }` and Next.js 15+ async-params `{ params: Promise<{ id: string }> }` shapes — the latter added 2026-05-23 after Phase A surfaced the regex gap), Fastify (Hono via fixture), tRPC, NestJS, FastAPI, Rails, Go chi with raw SQL. Rails reaches this detector only through the GitHub App path or a CLI run with `--ext=rb`: the CLI's default extension set is ts/tsx/js/jsx/py/go (`DEFAULT_EXTENSIONS` in `src/cli/file-walker.ts`), so a default `npm run scan` never reads a `.rb` file.
- **FastAPI idiomatic typed path param (Python slice 1b, 2026-05-28):** the gap slice 1 identified — `@router.get("/items/{item_id}")` + `def read_item(item_id: int)` where the id-typed handler argument (name `id` / `*_id` / `*Id`, type int/str/float/UUID) is the request-derived source, paired with the SQLAlchemy 2.0 / SQLModel `session.get(Model, id)` / `db.get(Model, id)` primary-key sink (added alongside the existing `.query.get` / `.filter_by` / Django-ORM sinks). Calibration n=5: positive (no ownership filter/check) 5/5 flag, negative (post-fetch `owner_id != current_user.id` check) 5/5 clear. Real-world validation (full-stack-fastapi-template) items.py read_item correctly cleared via its inline ownership check.
- **Tenant-scoping (orgId / tenantId across users) IDOR (H5, 2026-06-13):** a request-derived id fetched on an ORG-owned resource (model has `organizationId` / `org_id` / `tenant_id`, legitimately shared by many users in the org) with NO org filter in the query and no post-fetch org check — a user in org A reads org B's data. This is DISTINCT from the userId-ownership IDOR above (the correct fix is org-scoping, not user-scoping). The detector recognizes three SAFE scoping mechanisms and clears them: (a) **query-filter** — `where { id, organizationId: session.org }` (clears at high-confidence-safe); (b) **post-fetch field guard** — `if resource.org_id != caller.org_id: 403` (clears at high-confidence-safe); (c) **membership-table check** — a post-fetch `membership.findFirst({ userId, organizationId: resource.org })` + 403 (clears, but see caveat). **No prompt change was needed** — the shipping prompt's HIGH definition already names `organization_id`/`tenant_id` as ownable, and the LLM generalizes "ownership filter/guard" to the org axis correctly. **Calibration n=5 (log: `test-output/idor-tenant-2026-06-13.log`, fingerprint `5f5129f12b11`): 3/3 positives flag HIGH 5/5, 3/3 negatives clear 5/5, 30 calls 0 errors.** Frameworks/ORMs tested: Express+Prisma (TS), FastAPI+SQLAlchemy (Python). **Caveat (measured-honest), updated H6 2026-06-13:** the membership-table negative clears at MEDIUM-confidence-safe. H5 attributed this to context-window truncation of the `if (!membership) return 403` line and predicted that whole-file payload would firm it to LOW. **H6 measured that and the prediction was WRONG** (log: `test-output/idor-tenant-2026-06-13-h6.log`): under whole-file payload the truncation IS resolved — the reasoning is now complete and explicitly cites the full check ("looks up a membership record tying `req.user.id` to `project.organizationId`, returns 403 if none exists — an explicit ownership check after the fetch") — yet the confidence stayed predominantly MEDIUM (4/5 medium, 1/5 low). So the membership-table's medium-safe is the model's GENUINE confidence on an indirect, multi-step authorization pattern, **not** a truncation artifact. The verdict remains stable and correct (5/5 skip — both medium-safe and low-safe clear), so the claim holds; the caveat is corrected, not removed. The query-filter and post-fetch-field-guard shapes clear at firmer margins (low/high). Scoped to the tested shapes/frameworks; broader frameworks (tRPC/NestJS/Rails/Go) and cross-file scoping are not yet baseline-anchored — see DOES NOT CLAIM.

**DOES NOT CLAIM:**
- **Source and sink in different files (layered / repository-per-model architectures) — NOT covered, and no `SINK_PATTERNS` addition can close it.** When a route handler delegates data access to a repository / model / service module (`Knowledges.get_knowledge_by_id(id)`, `UserRepo.findById(id)`), the handler's file contains no query for any SINK pattern to match. `IdorDetector.analyzeFile` then returns on the `no source/sink co-occurrence` branch — before any candidate site is enumerated and before any LLM call — so the file produces no finding at any confidence. The sibling branch, whose `preFilterReason` is the `PROXIMITY_THRESHOLD` `lines apart` template, returns the same way when a source and a sink are both present but too far apart. **This is a file-boundary bound, not a pattern-coverage gap.** Widening `SINK_PATTERNS` cannot reach it: the only token present at the call site is the repository method itself, and a pattern matching `Capitalised.method(` would match most lines of most object-oriented code. **Measured 2026-08-17 at one pinned commit, `open-webui/open-webui@01f4282f1ffe0d6212f58d3afbeae21fffd0c4be`:** `routers/knowledge.py`, `routers/channels.py` and `routers/tools.py` yield 29 / 27 / 10 sources, **0 sinks, 0 pairs**, with no in-file query idiom and 100 / 140 / 52 repository-style calls; `models/messages.py`, in the same commit, yields 16 sinks and 16 pairs from the unmodified `SINK_PATTERNS` — which is what establishes the pattern list is adequate and the file boundary is the bound. **The MECHANISM is structural and read from source; the FREQUENCY is n=1 repository and is deliberately not asserted here.** The remedy would be cross-file dataflow: those three routers import 0% relative-dot and 100% absolute-package, the exact configuration §Cross-file guard resolution (H9) measured at ≈0% resolver reach and declined under a reopen trigger that is not met. **Invalidation: this row goes false if `enumerateSinkPairs` stops deriving both hit sets from a single file's content, or if `PROXIMITY_THRESHOLD` is removed. Re-audit it in the commit that changes either.** Evidence: `docs/measurements/idor-single-file-bound-2026-08-17.json`.
- IDOR via predictable filenames in storage paths (S3, GCS object IDs).
- **Cross-file tenant scoping** — org isolation enforced where the diff can't see it: tenant middleware that swaps in a pre-scoped client in another file (the `tenantPrisma`/`$extends` exception already covers this), RLS tenant policies in migrations (RLS sidecar exception), or a base repository/query class that auto-injects the org filter elsewhere. The H5 tenant claim above is single-file-diff shapes only.
- **Tenant scoping on frameworks beyond Express+Prisma / FastAPI+SQLAlchemy** — the org axis is not yet baseline-anchored on tRPC, NestJS, Rails, Go, or other ORMs (the object-ownership claim covers those; tenant-scoping there is plausible but unmeasured).
- GraphQL field-level IDOR.
- Business-logic IDOR ("user can change another user's email by knowing the email").
- TOCTOU race conditions on ownership checks.
- Files whose path carries a test-, fixture-, example-, script-, migration-, seed- or demo-named segment are never read by IDOR; the rule is shared by all six detectors and is disclosed once, with its measurement, under "Out of scope across the board" (the `SKIP_PATH_RE` row).

**Measured baseline:** 18/18 (9/9 positives ≥4/5, 9/9 negatives 5/5; 90 calls, 0 LLM errors) — current log `test-output/idor-baseline-2026-06-13-h6.log` (re-baselined under the H6 whole-file payload + multi-finding architecture; SYSTEM_PROMPT fingerprint `5f5129f12b11` unchanged, no verdict-rubric change). Prior on-disk baselines: `test-output/idor-baseline-2026-06-12.log` (windowed, lane-separation), `test-output/idor-day4-run.log` (16/16 at the then-16-fixture set).

**Multi-finding per file (H6, 2026-06-13):** the detector no longer judges only the closest source/sink pair — it enumerates ALL candidate sites (one per dangerous db-lookup sink, deduped by sink line, capped at 12/file with a surfaced log) and judges them together in ONE whole-file LLM call returning a verdict ARRAY (one verdict per pairIndex, each carrying the lane facts; per-pair verdict logic identical to the prior single-verdict path). This lifts the G3 one-finding-per-file false-negative: a file with two independent IDORs now reports two. Whole-file payload (200KB cap, window fallback over cap) replaces the 200-line window, so context like a post-fetch guard below the sink is never truncated — env-exposure and every non-route-def webhook trigger remain windowed; see their sections. **Calibration n=5 (log `test-output/idor-multi-2026-06-13.log`): fixture A (two independent IDORs) flagged exactly both sink lines {19,36} 5/5; fixture B (one real + one safe) flagged exactly the real one {22} with the safe pair absent 5/5 — multi-verdict perfectly stable, no count flips.** The 18-suite held 18/18 in the same state (no regression from the wider context), and the tenant suite held 6/6. This also unblocks H2's same-file introduced-vs-pre-existing partition (a single file with two findings at distinct lines can now be split by changed line).

**Lane discipline (2026-06-12):** the detector defers HIGH verdicts whose dominant defect belongs to a sibling lane — unauthenticated handlers (in-signature DI frameworks only, e.g. FastAPI) defer to auth-bypass; administrative operations (role/privilege mutation, arbitrary user management) defer to admin-check. The LLM reports structured lane facts; the suppression bound is deterministic detector code, fail-open on "unclear" (middleware-framework windows that cannot see auth still emit). Deferrals are logged to the review queue (`category: "idor-lane-deferral"`), never silently dropped. Acceptance: `test:idor-lane` 5/5 runs on the real-shape FastAPI corpus with the 18/18 baseline holding in the same state.

---

### 4. env-exposure (`env-exposure-multi`)

**CLAIMS:**
- `process.env` serialized into HTTP response body (Express `res.json(process.env)`, Fastify, Flask `jsonify`, FastAPI).
- `JSON.stringify(process.env)` in logger payload.
- Go `os.Environ()` echoed in handler response (`go_iter_environ`, `go_marshal_environ`). `os.LookupEnv` was listed here until 2026-09-10 and is REMOVED: no pattern, fixture or test names it, and a `LookupEnv` value echoed into a response matches nothing. See the first DOES NOT CLAIM row.
- Partial-redaction patterns with allowlist gaps.

**DOES NOT CLAIM:**
- **Env-dump shapes outside the literal pattern enumeration — NOT covered; a bound, mechanism-only.** `EnvExposureDetector` reaches the model only through twelve `PREFILTER_PATTERNS`: `.json(…process.env`, `.send(…process.env`, `return process.env`, `JSON.stringify(process.env`, `log|logger|console.*(…process.env`, `Object.keys|entries|values(process.env`, `return os.environ` / `dict(os.environ)`, `jsonify(…os.environ`, `return {…os.environ`, `json.Marshal|.Encode(…os.Environ`, `os.Environ()`. A dump that reaches a response through any other spelling — a spread `{ ...process.env }`, a `for (const k in process.env)` loop, `Object.assign({}, process.env)`, a `LookupEnv` echo — matches nothing, produces `no regex match`, and never reaches the model; there is no general "environment leaves the process" recognition. **No case is measured.** No qualifying handler exists in any corpus on disk (cal.com, ConvoForm, full-stack-fastapi-template hold no env dump in a handler at all); **that absence is not evidence of rarity, and the row says so.** A case, when one is found, is appended, not rewritten. **Invalidation: this row goes false if `PREFILTER_PATTERNS` gains a general env-reaches-response pattern. Re-audit it in the commit that changes the list.**
- **Judgment on a 24-line window, never the whole file — a bound, mechanism-only.** The model sees lines trigger-8 through trigger+16 around the EARLIEST pattern match (`extractContextWindow`; env-exposure is the only model-reaching detector with no whole-file path at all). An admin gate or production check more than 8 lines above the dump, or a redaction helper defined elsewhere in the file, is not in the payload, so the model judges the dump without the guard: the false-positive direction. A second dump in the same file is never judged (see the per-file bound under "Out of scope across the board"). Every recorded env-exposure request in `fixtures/replay/env-exposure-multi/` is 8-24 lines, because every fixture is short; the corpus has never exercised a guard outside the window. **No real-repository case is measured, and that absence is not evidence of rarity.** **Invalidation: this row goes false if env-exposure adopts `buildFunctionCodePayload` or any whole-file path.**
- Env vars leaking via error stack traces.
- Env vars in build-time bundles (Next.js `NEXT_PUBLIC_*` overlap is owned by secrets-exposure).
- Env vars leaked in webhook payloads to third-party services.
- Env vars logged at startup.
- Env vars baked into Docker image layers.
- Files whose path carries a test-, fixture-, example-, script-, migration-, seed- or demo-named segment are never read by env-exposure; the rule is shared by all six detectors and is disclosed once, with its measurement, under "Out of scope across the board" (the `SKIP_PATH_RE` row).

**Measured baseline:** 18/20 (log: `test-output/env-exposure-day5-r6-rerun.log`).

**ANNOTATION (2026-09-10) — 18/20 IS THE SCORE OF AN 11+9 SUITE UNDER A DISCARDED POLICY AND A RETIRED PROMPT, AND NO NUMBER EXISTS FOR THE SUITE THAT SHIPS TODAY.** The log records 11 positives and 9 negatives at n=5 (positives 9/11, negatives 9/9), `SYSTEM_PROMPT fingerprint: d2ca2f022d99`, scored under the policy in which a MEDIUM verdict was discarded before it could be reported — the policy #152 ended on 2026-08-07 — and under a prompt the shipping fingerprint has since moved away from. The number is kept because it is still the correct score of that suite under that policy and that prompt. **No number exists for the current 12-positive, 8-negative suite under the shipped policy:** `docs/measurements/env-exposure-stage3-2026-08-12.json` declines one (n=5 on a single fixture, n=1 on sixteen), and nothing since has measured the full suite. **One of the nine negatives in that 9/9 is `03-fastify-redacted-logs.ts`, later reclassified `positive/12` under R6 (`fixtures/env-exposure/META.md`):** its "correctly-skipped 5/5" was the discard policy masking a real miss, so a reader who sees "9 of 9 negatives" reads a cleaner result than the suite produced.

---

### 5. secrets-exposure (`secrets-exposure-multi`)

**CLAIMS:**
- Hardcoded API key / token / signing-secret literals: Stripe `sk_live_*` and publishable `pk_live_*`, OpenAI project keys `sk-proj-*` in the marker-bearing format (body carries `T3BlbkFJ`; the only project-key format on record as of 2026-09-10, and the annotation names what invalidates this item), Anthropic `sk-ant-api03-*`, Google `AIza*`, AWS `AKIA[A-Z0-9]{16}`, Slack webhook URLs, Postgres passwords in connection strings, hardcoded JWT signing secrets in source. **This list is the claim in full.** Matching is an EXHAUSTIVE enumeration of literal `PREFILTER_PATTERNS`, not a general notion of "secret": a credential shape absent from the enumeration is not detected, and no semantic judgment of "this string looks like a credential" happens anywhere. **The shipped path is regex-only and never calls the model** — `FIXOR_SECRETS_LLM_OPT_IN` defaults false, so `llmValidation` is false and a matching line is emitted directly at `confidence: "high"` / `severity: "critical"` carrying the pattern's hand-authored explanation, with no model adjudication of whether the match is a real credential. `OpenAI sk-*` was listed here until 2026-08-17 and is REMOVED: no pattern implements it, and a bare OpenAI key literal in source matches nothing (OpenAI is reachable only through the `NEXT_PUBLIC_*` row below, when the variable carries that prefix). See the first DOES NOT CLAIM row. **ANNOTATION (2026-09-10) — three items ADDED under rule 1, one restored in a form narrower than the one removed.** `sk-proj-*` is implemented by `openai_project_key` (positive `positive/16`, near-miss negative control `negative/11`, gate `test:secrets-exposure-prefilter` green at 16 of 16 patterns). The implementation requires a body of at least 48 characters carrying the literal `T3BlbkFJ` marker, and the item above claims exactly that and no more. **What the marker rests on:** no public OpenAI specification of key format exists, so the marker cannot be established from a primary source; it is base64 of "OpenAI" and is the shape encoded by third-party scanners, and gitleaks 8.24.3's `openai-api-key` rule requires it with 74- or 58-character segments on each side for `sk-proj-`, `sk-svcacct-` and `sk-admin-` keys. A reader of the item should expect an `sk-proj-` key copied from the OpenAI dashboard today to be caught, and should not expect a marker-less or sub-48-character project key to be caught, because none is on record and the item does not claim it. **Invalidation:** this item goes false the day OpenAI issues a project key without the marker or with a shorter body; re-audit it when gitleaks' `openai-api-key` rule changes shape, and when any customer-reported `sk-proj-` key fails to fire. The bare legacy `sk-*` shape stays unimplemented, and the 2026-08-17 REMOVED sentence stays true for it. `AIza*` (`google_api_key`, `positive/11`, near-miss control `negative/12`) and `pk_live_*` (`stripe_live_publishable`, `positive/12`, near-miss control `negative/13`) were implemented and fixture-covered since PR B but never advertised. Evidence: `docs/measurements/secrets-exposure-enumeration-bound-2026-08-17.json` records the pre-change asymmetry under `shippedButUnclaimed`.
- `NEXT_PUBLIC_*` exposing server secrets to client bundle.
- Supabase service-role key reaching client-side code.
- Firebase admin SDK creds in client components.

**DOES NOT CLAIM:**
- **Credential shapes outside the literal pattern enumeration — NOT covered, generic signing secrets included.** `SecretsExposureDetector` matches only its `PREFILTER_PATTERNS`; there is no general credential-shape recognition and, on the shipped path, no model stage that could supply one. A framework session-signing key assigned as a bare literal — Flask `app.config["SECRET_KEY"] = "…"`, Django `SECRET_KEY = "…"`, a `SIGNING_SECRET` or `whsec_*` constant — matches nothing, although each is a signing-secret literal in source and the CLAIMS row's own category head names that shape. **Measured 2026-08-17 at one pinned commit, `man-group/dtale@81578f4a256eef0bc9bde8bf1ca703d1e8d325de`** (the last commit carrying the literal; fixed in `32bd6fb4`, CVE-2024-3408): `dtale/app.py` sets `app.config["SECRET_KEY"] = "Dtale"`, a fixed Flask session-signing key that lets anyone forge session cookies, and the file yields **0 prefilter hits in total** — no pattern fires on that line or on any other. Control on the same instrument and language: `JWT_SECRET = "…"` and a `postgres://user:pw@host/db` URL both match, so the zero is the pattern set and not the harness. **The MECHANISM is exhaustive-enumeration matching and is read from source; the FREQUENCY of unmatched credential shapes in the wild is unmeasured and is deliberately not asserted.** **Invalidation: this row goes false if `PREFILTER_PATTERNS` gains a general credential-shape pattern, or if `FIXOR_SECRETS_LLM_OPT_IN` ceases to default false. Re-audit it in the commit that changes either.** Evidence: `docs/measurements/secrets-exposure-enumeration-bound-2026-08-17.json`.
- Secrets in git history (only present-tree files are scanned).
- Secrets in build outputs / bundles after the build runs (only source code, not artifacts).
- Secrets in `.env*` files — dropped by the extension gate (`.env`, `.env.local` carry no supported extension), not by the path rule; corrected 2026-09-10.
- **A credential sitting in a test, e2e, spec, script or fixture path is NOT detected by the secrets check, deliberately.** Files whose path carries a test-, fixture-, example-, script-, migration-, seed- or demo-named segment are never read by secrets-exposure, and since 2026-09-12 this detector's copy of the rule ALSO skips `script/` (singular), `e2e/` and `e2e-*/`, `api_tests/`, `api-tests/`, `app-tests/`, and any file named `*.test.<ext>` or `*.spec.<ext>` wherever it sits, a deliberate divergence from the five sibling copies recorded under "Out of scope across the board" (the `SKIP_PATH_RE` row, re-audited 2026-09-12). This is a MISS CLASS, stated here in its own words because the general row does not cover this detector's wider copy: people do commit working credentials into test files, and such a credential is invisible to this check. The trade, as measured: the widening removed 23 of 58 flags on 13 mature open-source repositories and 7 of 12 on 43 customer-shaped repositories, and none of the 30 removed was a real credential on those two corpora (`docs/measurements/fix-pairs-2026-09-11/secrets-false-alarms-remeasure-2026-09-12.json`). Two corpora are evidence for the trade, not a property of test files in general; the share of real credentials that live under these paths in any customer's code is unmeasured and is not asserted.
- Secrets stored in third-party config systems (Vault, AWS Secrets Manager) and incorrectly retrieved.
- Encryption-at-rest issues.

**Measured baseline:** 20/20 (log: `test-output/secrets-exposure-pilot-baseline.log`).

**ANNOTATION (2026-08-17) — 20/20 WAS MEASURED WITH THE LLM PATH ON, AND THAT IS NOT THE SHIPPED PATH.** The log records `SYSTEM_PROMPT fingerprint: 24b91c16fa48`, and all 10 positives are adjudicated `LLM:vuln/high` with model-written reasoning. The shipped path never constructs that call; it emits the matched pattern's hand-authored explanation instead. **The number is preserved because the flip IS verdict-preserving on this corpus, and the same log is what proves it:** all 50 negative runs were deflected before the model — 25 `pre-filter:no regex match`, 20 `pre-filter:server-only marker`, 5 `pre-filter:path filter` — so no negative verdict ever depended on model judgment. **That fact also bounds what the number can support.** The corpus contains no negative fixture that produces a prefilter hit and should nevertheless not be flagged, so 20/20 is silent on the false-positive behaviour of the bypass path — the path that ships, which emits every prefilter hit at high/critical with no adjudication. The reasoning TEXT also differs between the two paths and is not covered by this number.

EVIDENCE THIS DETECTOR CAN AND CANNOT PRODUCE. secrets-exposure is the only shipping detector with no model-reaching path: registry.ts constructs it with no options, llmValidation resolves false, and analyzeFile returns from the Option G bypass before the single callClaude site is reachable. Three consequences follow, and none of them is an omission. (a) There is no live accuracy number for the shipped path, and no runner that could produce one. src/test/test-secrets-exposure.ts and its test:secrets-exposure script were retired deliberately in cb11279 (PR #134, 2026-07-27), verified before removal to be a strict duplicate of the keyless gate, unable to reach the model under shipped defaults, and absent from test:ci, from the stage-3 selector and from every workflow. (b) There is no replay gate, and one is structurally impossible: with no callClaude request there is no request key to record a response under, which is why fixtures/replay/ holds no secrets-exposure-multi directory. (c) There is no stage-3 entry: the detector selector in stage3-live-detection.yml routes the six harness-routed gates and this detector is not among them. test:secrets-exposure-prefilter is therefore this detector's baseline — its sole automated guard, keyless, and stricter than the retired file on the one axis they shared, pinning per fixture the exact finding count, preFilterReason, flagged, type, severity, confidence and ruleId, plus zero LLM calls per fixture and globally. A CLAIMS row for this detector is paid for by that gate, not by a live run. If live accuracy is ever wanted (FIXOR_SECRETS_LLM_OPT_IN=true), build a runStabilityHarness caller like the other five detectors; do not resurrect the retired file, which was single-shot against the standing n=5 convention and hardcoded its denominators.

---

### 6. webhook-unverified (`webhook-unverified-multi`)

**CLAIMS:**
- Webhook handler routes for `/webhook`, `/hook`, `/hooks` in Express/Fastify-style routers, Flask decorators (`@bp.post`), and Go `HandleFunc` / named `*Webhook*` handlers. (Rails `post '/webhook'` routes are matched by a prefilter pattern — `rails_post_webhook` — and reach the LLM stage, but have **no Rails fixture** and are therefore not baseline-anchored; per rule 1 below this is a regex-reach note, not a CLAIMS row. Moved out of CLAIMS 2026-06-12; re-promote only with a positive + negative Rails fixture pair and a fresh baseline.)
- Detects: no signature verification at all; explicit env-flag bypass of verification (e.g. `WEBHOOK_VERIFY=off` short-circuits `constructEvent`); raw string-compare of signatures (`sig != expected`, classified MEDIUM as a timing-leak — see precision note below).
- Correctly recognizes (and skips) verification done via: `stripe.webhooks.constructEvent` / `stripe.Webhook.construct_event`, `@octokit/webhooks` + `createNodeMiddleware`, `twilio.validateRequest`, `crypto.timingSafeEqual`, Python `hmac.compare_digest`, Go `subtle.ConstantTimeCompare`, Go `hmac.Equal`, dedicated verification middleware mounted before the handler.
- Providers covered by fixtures (positive AND negative): Stripe, GitHub, Twilio, Lemon Squeezy, custom HMAC. Slack was listed here until 2026-09-10 and is moved out: it has a NEGATIVE fixture only (`negative/10-go-slack-hmac-equal.go`, Go `hmac.Equal` correctly skipped) and no positive, so a missed Slack handler is not baseline-anchored; per rule 1 below that is a regex-reach note, not a CLAIMS entry, until a positive fixture lands.
- **Next.js App Router and Remix v2 file-system-routed webhook handlers** (`export async function POST(req)` at `app/api/<provider>/webhook/route.ts`, or `export const action = ...` at `app/routes/webhook.<provider>.ts` / `app/routes/api.webhook.*.ts` for Remix): App Router and Remix prefilter patterns route every file-system-routed handler to the LLM (Remix bounded by the `isRemixRoutePath` filter described in the Track-2 framework-shape section), which then judges webhook-vs-not by file path (`/webhook/`, `/hooks/`, `/webhooks/` segment), webhook-library imports, signature-header reads (`stripe-signature`, `x-hub-signature`, `x-webhook-signature`, etc.), or `node:crypto` hash/HMAC operations whose output is compared against an incoming signature header. Hash outputs that flow into non-signature sinks (cache lookup, object-storage put, ETag header set, dedup-table insert, audit-log column, request fingerprinting for rate-limit memoization, content-addressed routing, etc.) are NOT webhook signals — Phase C tuned this distinction explicitly. Non-webhook App Router routes return LOW confidence and are not flagged. **Synthetic calibration:** Phase C measured 3 positive + 3 negative App Router fixtures, all 6/6 passed — including WH-P3 (custom-URL with `x-webhook-signature` header read, the symmetric webhook-recognition true-positive anchor) and WH-N1 (cache-key hashing, the load-bearing false-positive class). Baseline log: `test-output/webhook-unverified-app-router-phase-c-posttune.log`. **Phase E added 2 positive + 3 negative Remix fixtures** (Stripe no-sig + GitHub no-HMAC positives, Stripe constructEvent + GitHub timingSafeEqual + over-match utility under `app/utils/` negatives), all 5/5 passed; Phase F locked merge gate still passes (log: `test-output/phase-e-calibration-webhook.log`). **Real-world partial measurement:** the inbox-zero scan halted at **182 of 228 App Router routes (~80%, PARTIAL scan, halted for API budget — not a full-corpus sweep)**. Across the scanned 182 routes: **2 webhook_unverified findings**, both classified TRUE-POSITIVE-BY-RULE / FALSE-POSITIVE-BY-CONTEXT under documented limitations (see DOES NOT CLAIM rows below). The two findings are `apple/webhook/route.ts` (limitation class (c) cross-file verifier helper — verification delegated to `verifyAppleNotificationPayload` in `@/ee/billing/apple`) and `outlook/webhook/route.ts:59` (limitation class (d) non-HMAC shared-secret challenge — Microsoft Graph `clientState` equality comparison). Real-world log: `test-output/inbox-zero-api-scan-phase-d.stdout.log`. **Phase F update (2026-05-23):** the webhook prompt now recognizes both classes (see DOES NOT CLAIM rows (c) and (d) below). Outlook/webhook re-scan confirmed the drop to MEDIUM/review-queue. Phase F's first cut (PR #70, windowed payload) left a window-truncation residual on class (c): apple/webhook stayed HIGH because its verifier call sits at line 40, outside the ±8/+16 line window the webhook detector then shipped around the route declaration at line 17. **The structural follow-up (PR #71) closed that residual** by switching the webhook detector to whole-file payload for App Router route-def triggers, mirroring the discipline already in place for auth-bypass and admin-check. The real apple/webhook now drops to MEDIUM/review-queue: with whole-file context the LLM sees the awaited `verifyAppleNotificationPayload` call at line 40 and applies the cross-file-verifier MEDIUM rule. Both real-world inbox-zero routes (apple/webhook and outlook/webhook) are now measured-MEDIUM. Phase F + Path-A logs: `test-output/phase-f-real-world-confirm/`. **ANNOTATION (2026-09-10):** the whole-file payload applies ONLY when the route-def sentinel is the file's earliest prefilter match. An earlier content match (an import naming a webhooks module, a signature compare) takes the single trigger and the 24-line window instead; see DOES NOT CLAIM (e) for the measured bound.

**DOES NOT CLAIM:**
- Replay-protection issues independent of signature verification (timestamp window, nonce reuse).
- Key rotation / multi-secret handling.
- mTLS-based webhook auth (e.g., AWS API Gateway client certs).
- Files whose path carries a test-, fixture-, example-, script-, migration-, seed- or demo-named segment are never read by webhook-unverified; the rule is shared by all six detectors and is disclosed once, with its measurement, under "Out of scope across the board" (the `SKIP_PATH_RE` row).
- Providers not in prefilter or fixtures: Shopify (`X-Shopify-Hmac-Sha256`), Discord interactions (Ed25519), Mailgun, SendGrid, AWS SNS subscription confirmation, GCP Pub/Sub push (OIDC token), Square, PayPal, Paddle, Plaid, Algolia, Cloudflare Workers signed requests.
- **Critical scope limitations** (residual, post-App-Router prefilter extension):
  - **(a) Express-style URLs that don't carry the webhook/hook/hooks token.** The 5 router-style URL-name prefilter patterns still require the webhook URL string to appear in file content alongside router syntax. A real Stripe handler mounted at `app.post("/billing/events", ...)` (Stripe allows any URL) is invisible to those patterns and never reaches the LLM via the Express path. The App Router prefilter does NOT compensate (App Router uses different syntax). Affected: Express / Flask / Rails / Go custom-URL webhook handlers.
  - **(b) Non-App-Router DIY-HMAC handlers that are silent.** An Express / Flask / Rails / Go custom-HMAC handler that imports no webhook library AND exhibits no signature-comparison anti-pattern is still invisible to the content prefilter. App Router DIY-HMAC handlers ARE now reached by the LLM via the new `app_router_route_def` prefilter, where Phase A measured them silently skipped; Phase C calibrated the LLM's gated-vs-unguarded judgment on 3 positive + 3 negative synthetic App Router fixtures (see CLAIMS-row baseline log above). Phase D real-world partial measurement on 182 of 228 inbox-zero App Router routes (PARTIAL, halted for budget) returned 2 findings, both of documented-limitation classes (c) and (d) below. The same DIY-HMAC shape on Express / Flask / Rails / Go remains a residual content-prefilter gap regardless of Phase B or C.
  - **(c) Cross-file webhook verification helpers.** App Router webhook routes that delegate verification to a helper imported from another module (e.g., `await verifyXyzPayload(body.signedPayload)` from `@/ee/billing/xyz`). **Phase F + Path-A structural follow-up (2026-05-23) lifted this from HIGH to MEDIUM/review-queue.** The verifier's implementation cannot be confirmed cross-file, but the import + await + verify*-name-convention combination is strong enough signal that HIGH is over-confident. The structural follow-up (PR #71) switched the webhook detector to whole-file payload for App Router route-def triggers — matching the auth-bypass / admin-check discipline already in place — which closed the window-truncation residual Phase F's first cut left open. Measured success on both real inbox-zero routes: Outlook webhook (imports `getWebhookEmailAccount` from the verification-suggesting path `@/utils/webhook/validate-webhook-account`) and Apple App Store Server Notifications (awaits `verifyAppleNotificationPayload` from `@/ee/billing/apple` at line 40 — now visible to the LLM under whole-file context) both correctly route to MEDIUM/review-queue. The residual case is now bounded to the 200KB whole-file cap — pathologically large webhook handlers fall back to windowed payload — and to non-App-Router webhook triggers (express/flask/rails/go URL-name patterns keep their existing windowed payload because their prefilter signals are local).
  - **(d) Non-HMAC shared-secret challenge verification.** Webhook handlers using shared-secret challenge mechanisms instead of HMAC signature verification — notably **Microsoft Graph webhooks** (compare `notification.clientState` against an env-stored expected value, 403 on mismatch). **Phase F (2026-05-23) lifted this from HIGH to MEDIUM/review-queue.** The webhook prompt now recognizes the body-field-compare-against-env-value-with-unauthorized-status pattern as a shared-secret verification shape (the documented Microsoft-Graph subscription-validation pattern), routes it to MEDIUM, and the LLM reasoning explicitly names the field, env value, and 403 response. Measured Phase F success: inbox-zero's Outlook (Microsoft Graph) webhook handler at `outlook/webhook/route.ts` correctly dropped to MEDIUM/review-queue. The env value cannot be confirmed cross-file, which is why the routing is MEDIUM rather than LOW. Other shared-secret challenge mechanisms (custom integrations, internal services) using the same body-field-compare-env-and-unauthorized-on-mismatch shape will route the same way. Class (d) has no observed window-truncation residual in the Phase F measurement: the compare happens close to the route declaration in the outlook real-world case.
  - **(e) Earliest-trigger selection defeats whole-file payload — a bound, measured.** `prefilterRegex` returns the single earliest match in the file across all patterns, and only a route-def sentinel (`app_router_route_def`, `remix_handler_def`) in that position earns the whole-file payload. Whenever any content pattern matches earlier in the file — `webhook_lib_import` on an import line is the common case — that match is the trigger, the payload is the ±8/+16 window around it, and the handler below is never shown to the model. **Measured 2026-09-10 at one pinned commit, `calcom/cal.com@fb0149453e97a047f44ec21e99f9d8af420d8365`:** `apps/web/app/api/recorded-daily-video/route.ts` is 239 lines and exports `POST` at line 239; the shipped detector selects the import at line 36 (`} from "@calcom/web/lib/daily-webhook/triggerWebhooks";`) and sends lines 28-51. `postHandler` begins at line 54 and its `x-webhook-signature` compare sits at lines 71-73, outside the payload. Captured from the shipped `WebhookUnverifiedDetector` with the model entry point intercepted at the module boundary; the same instrument reproduces, byte for byte, the recorded request for `positive/10-go-github-eq-compare.go` in `fixtures/replay/webhook-unverified-multi/`, which is its control. **The MECHANISM is earliest-wins selection and is read from source; the FREQUENCY of App Router webhook files with an earlier content match is unmeasured and is deliberately not asserted.** **Invalidation: this row goes false if `prefilterRegex` stops selecting the earliest match, if route-def sentinels gain priority over content patterns, or if the non-route-def window changes shape. Re-audit it in the commit that changes any of these.** Evidence: `docs/measurements/webhook-earliest-trigger-bound-2026-09-10.json`.
  - **(f) Windowed judgment on every non-route-def trigger — a bound, mechanism-only.** Express / Flask / Rails / Go URL-name triggers and all content triggers are judged on lines trigger-8 through trigger+16 (`extractContextWindow` via `buildFunctionCodePayload`). A verification call more than 16 lines below the route line — `constructEvent` after a long body read, a `timingSafeEqual` at the end of a long handler — is not in the payload, so the model judges a handler as unverified that is verified further down: the false-positive direction. **No case is measured.** No qualifying file exists in any corpus on disk (cal.com, ConvoForm, full-stack-fastapi-template); cal.com's two payment webhooks, `btcpayserver/api/webhook.ts` and `hitpay/api/webhook.ts`, are Pages Router `export default handler` files with the URL outside the content and never reach the prefilter at all, which is classes (a)/(b) above. **That absence is not evidence of rarity, and the row says so.** A case, when one is found, is appended here, not rewritten. **Invalidation: this row goes false if non-route-def triggers gain the whole-file payload or the window changes shape.**

**Precision note (cross-detector), REWRITTEN 2026-08-07 — the policy it described no longer ships.** MEDIUM-confidence findings (timing-leak comparisons, env-flag-conditional bypass) ARE now emitted, carrying `confidence: "medium"` on the finding. Severity is unaffected: severity says how bad it would be if real, confidence says how sure the model is that it is real. Until 2026-08-07 the detector returned an empty array before any `Finding` was constructed, after writing one `logger.warn` nothing consumed — the HIGH-only emit policy, applied identically by all six detectors. It was ended because the measured MEDIUM lane turned out to contain four true positives and two correct uncertainties and **zero false positives**, so the policy was paying four real vulnerabilities to hide none.

**There was never a review queue, and now nothing needs one.** The `*-review-queue` category strings appeared only at the six detector emit sites; nothing consumed them. There was no table, no dashboard view, no digest, no notification, and the verdict was not retained anywhere queryable — the emit half of the policy shipped and the route-to-a-destination half never did. Rather than build the destination, the finding now goes where every other finding goes: to the customer, labelled with the model's own confidence. The log categories are now `*-medium-emitted`; keeping a name that pointed at a destination which never existed, on a path that now reports, would have been actively misleading.

If a customer asks "will Fixor flag a Stripe handler that toggles signature verification on/off via an env flag?", the honest answer since 2026-08-07 is *"yes, at medium confidence — you will see the finding and you will see that the tool is not certain."* **This answer has now been wrong twice in three days, in opposite directions**, which is why the full history stays attached: it read *"flagged in the review queue, not in the PR comment"* (asserted a destination that does not exist; corrected 2026-08-04), then *"no — MEDIUM verdicts are discarded without being reported to you"* (true as of 2026-08-04, false as of 2026-08-07). Live evidence for the middle version: run `30903038957` produced 15 `isVulnerable:true @ medium` verdicts on the env-exposure corpus, every one discarded.

**CORRECTED AGAIN 2026-08-07, and the correction runs the other way.** The sentence above ended *"including 5 on a negative fixture where the discard masked a false positive."* That is withdrawn. The fixture — env-exposure `negative/03-fastify-redacted-logs.ts` — was mislabelled; the model was right. It is now `positive/12` under R6, and the discard masked a **true** positive. Across the whole recorded MEDIUM lane (6 entries, two corpora, `npm run test:recorded-medium-census`) there are **four true positives and two correct uncertainties, and zero false positives**. The customer-facing answer above is unchanged and if anything understates the cost: what is discarded is not a mixed bag of good and bad findings but, on all evidence held, real vulnerabilities. Case: `fixtures/env-exposure/META.md`, R6 #2.

**Measured baseline:** 29/30 (Phase F + Path-A post-tune; log: `test-output/webhook-unverified-path-a.log`, 2026-05-23). Phase F first-cut baseline preserved at `test-output/webhook-unverified-phase-f.log` (same 29/30, different payload shape — Phase F shipped windowed, Path-A ships whole-file for App Router route-def). Pre-Phase-F baseline was 24/26 (Phase C post-tune; log preserved at `test-output/webhook-unverified-app-router-phase-c-posttune.log`). Phase F added 4 symmetric-anchor fixtures (positive 14/15 + negative 14/15) for classes (c) and (d) above. Post-tune Phase F result: 14/15 positives flagged (including new POS-14 apple-cross-file-no-call and POS-15 graph-clientstate-no-compare both correctly HIGH; the single positive miss is POS-10 go-github-eq-compare with MEDIUM-suppressed timing-leak reasoning, unchanged from Phase C); 15/15 negatives correctly skipped (including new NEG-14 cross-file-verifier-helper and NEG-15 clientstate-challenge both correctly routed to MEDIUM/review-queue per the locked merge gate in `src/test/test-webhook-unverified.ts`). Pre-Phase-C Express-only baseline preserved at `test-output/webhook-unverified-baseline.log`. **Phase F behavior shift on existing fixture:** POS-04 `04-stripe-verify-toggle.js` moved from MEDIUM-suppressed (pre-tune) to HIGH-FLAG (post-tune); diagnostic re-run at `test-output/phase-f-real-world-confirm/pos04-diagnostic.log` confirmed the post-tune HIGH reasoning correctly identifies the env-flag conditional bypass at runtime (security-positive shift, the more specific MEDIUM definition narrowed when MEDIUM is correct). Real-world partial measurement added 2026-05-23: `test-output/inbox-zero-api-scan-phase-d.stdout.log` — 182 of 228 inbox-zero App Router routes scanned (PARTIAL, halted for budget). Phase F real-world re-scan (windowed payload, PR #70): outlook/webhook correctly dropped to MEDIUM/review-queue, apple/webhook stayed HIGH due to window-truncation. **Path-A real-world re-scan (whole-file payload, PR #71):** both apple/webhook AND outlook/webhook now route to MEDIUM/review-queue — apple specifically because the LLM now sees the `verifyAppleNotificationPayload` await at line 40 under whole-file context. Log: `test-output/phase-f-real-world-confirm/path-a-closure.log`.

---

## Out of scope across the board

Independent of any specific detector, Fixor explicitly does not:

- **Replace SAST.** No SQL injection / XSS / command injection / path traversal scanning (those detectors are suppressed at output time — see `src/config/finding-suppressions.ts`).
- **Replace dependency scanning.** No CVE / SCA / supply-chain detection. Snyk and Dependabot remain authoritative.
- **Replace secret scanning of git history.** GitHub native secret scanning, gitleaks, etc. cover history; Fixor scans the present-tree diff.
- **Cover infrastructure-as-code or container security.** No Terraform, no Kubernetes manifests, no Dockerfile scanning.
- **Provide compliance certification.** SOC 2 / ISO 27001 outputs require their own controls; Fixor's PDF/SARIF outputs are evidence inputs to a compliance program, not compliance themselves.
- **Read any file whose path carries a test-, fixture-, example-, script-, migration-, seed- or demo-named segment — a bound shared by all six detectors, measured.** Each detector's `detect()` applies the same `SKIP_PATH_RE` — segments `test`, `tests`, `__tests__`, `spec`, `fixtures`, `example`, `examples`, `scripts`, `dev-tools`, `migration`, `migrations`, `seed`, `seeds`, `demo`, case-insensitive, matched as whole path segments — before its prefilter runs, and drops the file with `preFilterReason: "path filter"`. A file NAMED `spec.ts` is scanned; a directory named `spec` is not. The six regexes are byte-identical copies, not a shared constant. Neither entry point adds or removes anything here: the CLI walker's own deny-list is dependency, build and editor directories only, and the GitHub App path filters nothing by path. Until 2026-09-10 this was disclosed only as two secrets-exposure lines, as if it were that detector's own rule. **Measured 2026-09-10 with the shipped `SecretsExposureDetector` gate (same regex; that detector has no model path, so the run is $0.00 by construction) at three pinned commits:** `fastapi/full-stack-fastapi-template@cd83fc10ca20393e9ee50e3005e170c6929e047e`, 30 of 142 supported-extension files dropped (29 under `tests/`, 1 under `scripts/`); `calcom/cal.com@fb0149453e97a047f44ec21e99f9d8af420d8365`, 264 of 5049 dropped, of which 61 sit outside any test segment, including API route handlers under `packages/platform/examples/base/src/pages/api/` and `apps/api/v2/scripts/generate-secrets.ts`; `growupanand/ConvoForm@c011c18b342606fcb2c79d65bd2296aecdf0d64d`, 31 of 550 dropped. Controls on the same instrument in each run: `tests/a.ts` reports `path filter`, `src/routes/a.ts` does not, and the instrument's dropped set is set-equal to an independent regex copy's. **The MECHANISM is read from source; the SHARE of a given customer's first-party code that lands under these segments is unmeasured and is deliberately not asserted.** **Invalidation: this row goes false if any of the six regexes diverges from the others, if a shared constant replaces them, or if either entry point gains a path rule. Re-audit it in the commit that changes any of these.** Evidence: `docs/measurements/skip-path-cross-detector-bound-2026-09-10.json`. **RE-AUDITED 2026-09-12 under this clause: the secrets-exposure copy DIVERGED, deliberately and on the record.** Five detectors (auth-bypass, admin-check, idor, env-exposure, webhook-unverified) still carry the byte-identical regex above, and everything measured in this row about the shared rule holds for those five. The secrets-exposure copy alone now also drops the segments `script` (singular), `e2e`, `e2e-<anything>`, `api_tests` / `api-tests`, `app-tests`, and any file whose NAME ends `.test.<ext>` or `.spec.<ext>` wherever it sits (so `auth.service.spec.ts` beside its service is now skipped by secrets-exposure only). Why only that copy: the other five detectors' fix-pair measurements are in flight on the old rule, and changing their instrument mid-measurement is the failure the sequencing ruling on L-023 forbids; design (a) of `docs/measurements/fix-pairs-2026-09-11/secrets-skip-list-prereg-2026-09-12.md`. Measured effect of the secrets copy, pre-registered from the flagged paths then observed on the two false-alarm corpora with the unchanged harness: `docs/measurements/fix-pairs-2026-09-11/secrets-false-alarms-remeasure-2026-09-12.json`. Go `*_test.go` is NOT in either copy; pre-registered as its own step. The three pinned repositories above were not re-scanned with the secrets copy (two of the three commits are not on disk); the secrets copy's own bound is the two-corpus re-measure.
- **Report more than one finding per file from auth-bypass, admin-check, env-exposure, or webhook-unverified — a bound, mechanism-only.** Each of those four selects one trigger per file (auth-bypass: the first route-def hit, else the first line hit; the other three: the earliest match in the content), makes one model call on it, and emits at most one finding. A second defect in the same file is never judged. IDOR is the exception (H6 multi-finding, capped at 12 sites per file); secrets-exposure emits per matching line. No case is measured and none is needed: the selection is deterministic and read from source. **Invalidation: any of the four gains multi-trigger judgment.**
- **Route-based detection is scoped along two axes — language and framework shape — both matter.**
  - **Language:** auth-bypass's missing-middleware / missing-HOC-wrapper sub-claims and admin-check's missing-admin-gate / missing-admin-HOC-wrapper sub-claims are JS/TS only today. Their sentinel-string and hardcoded-admin sub-claims are content-based and run across the languages listed in their CLAIMS rows. IDOR, env-exposure, secrets-exposure, and webhook-unverified have broader language coverage as enumerated in their CLAIMS rows.
  - **Framework shape:** auth-bypass and admin-check cover router-style (`router.METHOD(path, handlers...)` — Express family) AND **Next.js App Router** method-named exports (`export const POST = ...` / `export async function GET(...)`) AND **Remix v2** file-system-routed handler exports (`export const loader = ...` / `export const action = ...`, added Phase E 2026-05-23) via three sibling prefilter patterns (`express_route_def`, `app_router_route_def`, `remix_handler_def`). The router-style sub-claim has full baselines (`test-output/auth-bypass-post-pr53-baseline.log`, `test-output/admin-check-post-pr53-baseline.log`); the App Router sub-claim has Phase C synthetic calibration baselines (see per-detector CLAIMS rows) PLUS a Phase D real-world partial measurement against 182 of 228 inbox-zero App Router routes (~80%, PARTIAL scan, halted for API budget; log: `test-output/inbox-zero-api-scan-phase-d.stdout.log`). **Remix v2 sub-claim — Phase E (2026-05-23):** the regex `REMIX_HANDLER_DEF_RE` matches `(loader|action)` exports, bounded by an `isRemixRoutePath(filePath)` path-aware filter (`/routes/` segment or `app/root.{ts,tsx}`) that mirrors detector-side and estimator-side semantics so utility modules under `app/lib/` / `app/utils/` exporting `loader`/`action` factory functions are not misrouted to the LLM. The path filter bounds the over-match risk on `loader`/`action` (also a Remix data-fetch utility convention, not only a route handler) on the file-system-routed side. **Synthetic calibration:** 17 Remix fixtures across the 3 route-shape detectors (auth-bypass 6, admin-check 6, webhook 5, including over-match utility anchors), all passed (logs: `test-output/phase-e-calibration-{auth-bypass,admin-check,webhook}.log`). **End-to-end demo:** the fixor-demo-remix planted-vuln scan flagged 3 planted Remix-shape vulnerabilities with correct detector attribution and LLM reasoning that explicitly named "Remix `action` export" (log: `test-output/phase-e-demo-remix-scan.md`). **Two named residuals on the Remix claim at the point of this claim** (Phase D-style measured + caveated discipline): (i) the **real-world Remix production-corpus baseline (e.g. against Trigger.dev's 411 routes) is deferred** — calibration + demo were scientifically sufficient under the same partial-sample reasoning that closed Phase D; trigger to open: Anthropic balance recovers above ~$10 OR a Remix customer signal (see `project_fixor_phase_e_remix_extension` memory's "Deferred Trigger.dev baseline" section); (ii) **non-conventional wrapper-name divergence on unknown Remix codebases is a known residual** until a real corpus is measured — calibration covers `withAuth`, `withAdmin`, `withLogging`, `withSessionAnalytics`, `requireUser`, `getServerSession`, but a customer codebase using a deceptive non-conventional wrapper name could surface a false positive; the path-aware filter **bounds, not eliminates**, the divergence class. webhook-unverified covers Express + Next.js App Router + Remix v2 shapes via the same three sibling prefilter patterns plus an LLM-stage webhook-vs-not judgment driven by file path metadata / `node:crypto` HMAC / signature header reads — see the webhook-unverified "Critical scope limitations" section above for the four residual gaps (a) URL-in-content, (b) DIY-HMAC silent on non-route-shape, (c) cross-file verifier helpers (Phase F + Path-A structural follow-up 2026-05-23: lifted to MEDIUM/review-queue, whole-file payload for route-def triggers closed the window-truncation residual), (d) non-HMAC shared-secret challenge (Phase F 2026-05-23: lifted to MEDIUM/review-queue). The webhook detector ships whole-file payload for an App Router or Remix route-def trigger only when that trigger is the file's earliest prefilter match; otherwise it is windowed like every other trigger (webhook-unverified DOES NOT CLAIM (e), measured; (f), mechanism-only). IDOR, env-exposure, and secrets-exposure are framework-shape independent by design (content-based detection with framework-aware patterns including IDOR's `nextjs_destructured` — extended 2026-05-23 to cover Next.js 15+ async-params — and secrets-exposure's `NEXT_PUBLIC_*`); their CLAIMS rows enumerate the verified patterns and their content-based detection still runs on Remix file content normally.

## Cross-detector overlap

Detectors run independently and their findings are reported independently. The only post-processing is `collapseFindings` (`src/cli/finding-merge.ts`, applied in `scan.ts`), which collapses **exact duplicates only**: two findings that share the same `(file, line, type)` triple are the same finding reported twice and are merged to one. **Distinct vuln types at the same `file:line` are preserved as separate findings; there is no cross-detector suppression.**

A prior step (`suppressAdminCheckWhereAuthBypass`) dropped every `admin-check` finding on a `file:line` where `auth-bypass` also fired, on the theory that "no auth at all" subsumes "no admin gate." That assumption is false for an authenticated-but-not-admin route (an admin action gated only by a generic current-user dependency, with no superuser check). There, `admin-check` is the correct, more-specific finding and `auth-bypass` is the cross-fire, and the suppression silently deleted the correct finding while keeping the mislabeled one. The real-shape FastAPI proof reproduced exactly this on the admin role route, so the step was removed and a regression test (`src/test/test-finding-merge.ts`) now guards against re-introducing it.

**Consequence (a known limitation, not a defect):** because nothing is suppressed across detectors, one vulnerable route can draw two or three findings with different labels, and a cross-wired finding's mechanism description can be imprecise. That labeling work is tracked but not yet shipped. This is why a multi-detector scan emits more findings than there are vulnerable routes (finding-count inflation), with no effect on the false-positive count on safe routes. Visible cross-fire noise is self-announcing and is addressed at the detector/prompt layer, never by deleting findings. **Update (H7, 2026-06-13):** the auth-bypass↔admin-check cross-fire on authenticated-admin routes (auth present, no admin gate) is now resolved at the detector layer — auth-bypass reports lane facts and defers that shape to admin-check (see auth-bypass §Lane discipline), so the route draws one correctly-labeled admin-check finding instead of a cross-fired pair. This is the same mechanism IDOR already uses to defer to its siblings; it stands down the less-specific detector via its own verdict, and still never deletes another detector's finding.

## Verdict-layer escalation (H8, Phase H Tier 3, 2026-06-13) — EXPERIMENTAL, default-OFF

**Status: mechanism-validated, NOT accuracy-validated. The flag ships OFF and stays OFF for real scans.** This section does not add or widen any CLAIMS — it documents an internal capability that is wired but not enabled.

**What it is.** A MEDIUM-confidence verdict from any of the six detectors is otherwise discarded without being emitted or retained, leaving only a `logger.warn` line that nothing consumes (the audit's "fuzzy middle"; see the Precision note above for why "review queue" is a misnomer). When `FIXOR_ESCALATE_MEDIUM=true`, that MEDIUM is re-asked to a stronger second model (Opus 4.8, `claude-opus-4-8`) with WHOLE-FILE context and a structured promote/clear/uncertain question, through one shared module (`src/analysis-engine/verdict-escalation.ts`, `resolveMediumVerdict`). The detection model stays Sonnet 4.6; escalation is a bounded, flagged second pass reached only on a MEDIUM (single digits per corpus).

**Deterministic handling, non-deterministic model.** Opus 4.8 has no `temperature:0` and is not run-to-run deterministic. What is deterministic is the mapping of its structured `decision`: `promote_to_high → emit HIGH`, `clear → drop`, and a **total fail-safe** — `still_uncertain`, refusal, no tool_use, timeout, http_error, parse_error, or a missing API key all collapse to `review-queue` (today's behavior). The escalation call is tagged `coverage:"auxiliary"`, so an escalation failure never degrades scan-coverage integrity.

**Flag OFF (default) is inert.** `resolveMediumVerdict` returns `review-queue` on its first line, before any client is constructed or any call is made — proven by the off-path acceptance test (`lastEscalationDiag` stays null). The six detector MEDIUM branches behave byte-identically to pre-H8; `test:ci` is green with the flag unset. IDOR keeps its own per-pair `continue` control flow and source/sink telemetry — the shared module returns a uniform decision, it does not flatten that difference.

**What "validated" means here (binding scope).** Validation is the 4-anchor falsifier in `src/test/test-h8-escalation-anchors.ts` (`npm run test:h8-escalation`, K=5 replays each, any flip = fail; log: `test-output/h8-escalation/anchors-k5-run1.json`, $0.26 spend):
- `POS-15` outlook no-compare → **promote** (5/5) — real vuln rescued from the queue.
- outlook neg/15 clientState challenge → **clear** (5/5) — in-file compare+403 recognized.
- H5 idor-tenant neg/02 membership → **clear** (5/5) — in-file membership check recognized as tenant scoping.
- apple neg/14 cross-file verifier → **stay-uncertain** (5/5) — the binding negative control: the verifier body is cross-file, so escalation must NOT clear; it correctly refused on every replay.

This authorizes ONLY **"the wiring is correct and fail-safe."** It is **NOT an accuracy claim.** Four anchors (1 promote / 2 clear / 1 uncertain) exercise all three branches but cannot substantiate an escalation accuracy number — that requires a proper MEDIUM corpus (≥8–10 cases/lane with symmetric pins, run K-of-N) which does not exist yet. K=5-with-zero-flips bounds only gross instability; a true stability claim would need K≈20+. Until that corpus exists, the flag is experimental and default-off; do not flip it on for real scans and do not advertise escalation on any public surface.

## Cross-file guard resolution (H9, Phase H Tier 3, 2026-06-14) — DESIGNED + MEASURED, NOT BUILT

**Status: measured, not pursued. No code shipped.** This section adds no CLAIMS and no DOES NOT CLAIM rows (the deceptive-stub / generic-HOC cross-file limitations are already enumerated under auth-bypass and admin-check DOES NOT CLAIM, and remain the truthful posture). It is a decision record so the next person does not re-propose H9 from scratch.

**What H9 would have been.** Deterministically follow the import of a named guard wrapper / FastAPI dependency (`withAuth` from `./auth`, `Depends(verify_x)`), read the referenced file, and inject the guard's actual body as a sidecar so the LLM judges the route with the wrapper's real behavior instead of trusting its name — closing the deceptive-stub gap (a `withAuth` that is really `(fn) => fn`) and the generic-named-wrapper false positive. It would have reused the existing sidecar channel and prompt blocks; the net-new part was a **relative-only** import resolver.

**Why it was not built (zero-spend measurement, evidence: `test-output/h9-reach/` — `measure.mjs` + `RESULTS.md`, gitignored per the `test-output/` convention).** A deterministic resolver-reach measurement over calcom / convoform / documenso / full-stack-fastapi-template found that a relative-only resolver reaches **≈ 0% of real cross-file auth guards**:
- TS App Router / Remix routes (calcom, convoform, documenso) have **zero** cross-file auth-HOC-wrapped routes — auth is inline or via node_modules; the only captured wrappers are non-auth (`defaultResponderForAppDir`, `createThemeAction`), imported via baseUrl/alias paths. `withApiAuth` (×77 in calcom) is **test scaffolding** (`*.e2e-spec.ts`), and `apps/api/v2` is NestJS Guards (already DOES NOT CLAIM).
- The FastAPI template — the one corpus where the cross-file `Depends` pattern is real and dense — imports **100% absolute-package** (`from app.api.deps`), **0%** relative-dot. Relative-only reaches none of it.
- **Zero deceptive stubs** were found in any corpus (every visible guard body was genuine), reinforcing the prior corpus grep (0 `withAdmin*` stubs across 5 repos) and the 182-route inbox-zero scan (0 findings). The false-negative H9 targets remains unobserved in the wild.

Reach ≈ 0% is structural, not bad luck: baseUrl/alias (TS) and absolute-package (Python) are the *dominant* import styles, so the fail-open/unresolvable bucket is the common case. H9 is also the most expensive Tier-3 item (~$25–50 to calibrate, vs H8's $0.26), giving it near-zero detection upside for the highest cost. Measurement confidence: HIGH on the lopsided relative-vs-alias split (cleanly static); SUGGESTIVE not conclusive on generalization (N=4 small template/SaaS clones; inbox-zero, the largest, is not on disk).

**Reopen trigger (binding).** Reopen H9 ONLY if a real customer repo surfaces an actual no-op `withAuth`/`withAdmin` stub (or a generic-named wrapper that really enforces auth, producing a measured false positive). At that point build the **FULL** resolver — tsconfig `paths`/`baseUrl` + Python package-root + barrel-following — which is the only version that reaches real-world imports. **Never ship the relative-only resolver**; the measurement shows it would fire on toy fixtures and nothing else.

## When this file changes

Three rules:

1. **Adding a CLAIMS row** requires a positive fixture in `fixtures/<detector>/positive/` and at least one negative-control fixture in `fixtures/<detector>/negative/` exercising the new pattern. The fixture suite must pass a fresh baseline run before the CLAIMS row is added.
2. **Adding a DOES NOT CLAIM row** can ship ahead of code — it's a credibility-defending negative claim, not a capability claim. Add it the moment a real customer or reviewer raises the question, so the next reviewer doesn't have to re-ask.
3. **Promoting a detector to the wedge** (going from N to N+1 advertised classes) requires (a) a saved baseline log in `test-output/`, (b) a new CLAIMS / DOES NOT CLAIM row in this file, and (c) an audit of every customer-facing surface so the count stays consistent. See the audit pattern used in PR #54 (`docs/promote-webhook-unverified-6-class-wedge`).

If a public surface contradicts this file, this file wins and the public surface is the bug.
