{
  "version": "1.0",
  "results": [
    {
      "check_id": "javascript.express.security.audit.sql-injection",
      "path": "src/unsafe.js",
      "start": { "line": 4, "col": 1 },
      "end": { "line": 6, "col": 4 },
      "extra": {
        "message": "Unsanitized query: dynamic SQL built with string concatenation",
        "metadata": {
          "category": "sql-injection",
          "cwe": ["CWE-89"],
          "technology": ["sql", "nodejs"]
        },
        "lines": "  return pool.query(\n    \"SELECT * FROM orders WHERE tenant_id = \" + tenantId + \" LIMIT 100\"\n  );"
      }
    }
  ]
}
